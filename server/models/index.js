const { Gym } = require('./gym');
const { Workout } = require('./workout');
const { User } = require('./user');
const { Admin } = require('./admin');

module.exports = {
    Workout,
    Gym,
    User,
    Admin,
};
